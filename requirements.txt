# MLX Parallels - Complete Requirements
# Optimiert für Apple Silicon und MLX-LangChain-Lite Integration

# Core MLX Dependencies
mlx>=0.25.2
mlx-lm>=0.24.0
mlx-vector-db

# Python Core Libraries
numpy>=1.24.0
psutil>=5.9.0

# Async and Networking
aiohttp>=3.8.0
asyncio-throttle>=1.0.2

# Data Processing
pandas>=2.0.0
pyarrow>=12.0.0

# Web Framework (optional - für API Server)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Logging and Monitoring
structlog>=23.1.0
rich>=13.0.0

# Configuration Management
pydantic>=2.0.0
python-dotenv>=1.0.0

# MLX-LangChain-Lite Specific Dependencies
# (Diese sollten mit Ihrem bestehenden Projekt übereinstimmen)

# Document Processing
pymupdf>=1.23.0  # fitz für PDF-Verarbeitung
python-docx>=0.8.11  # für DOCX
markdown>=3.4.0
beautifulsoup4>=4.12.0
newspaper3k>=0.2.8
feedparser>=6.0.10

# Web Scraping (für Research Assistant)
selenium>=4.15.0
requests>=2.31.0

# Code Analysis
lizard>=1.17.0  # für Code-Komplexität
tree-sitter>=0.20.0  # für Code-Parsing

# Vector Operations (falls nicht über mlx-vector-db abgedeckt)
faiss-cpu>=1.7.4; platform_machine != "arm64"  # Fallback für Intel Macs
scipy>=1.11.0

# Utilities
tqdm>=4.66.0
click>=8.1.0
pathlib2>=2.3.7; python_version < "3.6"

# JSON Processing
orjson>=3.9.0  # Schnellere JSON-Verarbeitung

# Memory Profiling (Development)
memory-profiler>=0.61.0
pympler>=0.9

# Optional: Enhanced Logging
loguru>=0.7.0

# Optional: Progress Bars
enlighten>=1.12.0

# Optional: Configuration Validation
jsonschema>=4.19.0

# Optional: Performance Monitoring
py-spy>=0.3.14

# MLX Community Models Support
huggingface-hub>=0.17.0
transformers>=4.35.0
tokenizers>=0.15.0
safetensors>=0.4.0

# Optional: Visualization (für Performance-Monitoring)
matplotlib>=3.7.0
plotly>=5.17.0

# Optional: Advanced Analytics
scikit-learn>=1.3.0

# Development Dependencies (nur für Entwicklung)
black>=23.9.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.6.0
pre-commit>=3.4.0

# Platform-specific optimizations
# Für Apple Silicon
mlx-whisper>=0.1.0; platform_machine == "arm64"  # Optional: Für Audio-Processing

# Dokumentation (optional)
mkdocs>=1.5.0
mkdocs-material>=9.4.0
mkdocstrings[python]>=0.23.0

# Testing Performance
pytest-benchmark>=4.0.0
pytest-xdist>=3.3.0  # Parallel testing

# Memory Testing
pytest-memray>=1.5.0; platform_machine == "arm64"

# Optional: Database Support (falls erweiterte Persistierung benötigt)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Optional: Redis Support (für Caching)
redis>=5.0.0
aioredis>=2.0.0

# Optional: Message Queue Support
celery>=5.3.0
kombu>=5.3.0

# Security
cryptography>=41.0.0

# Environment Management
python-decouple>=3.8

# Zu deiner bestehenden requirements.txt hinzufügen:
pyyaml>=6.0.1          # Für Configuration System
httpx>=0.25.0          # Modernerer HTTP Client
aiofiles>=23.2.0       # Async File I/O
diskcache>=5.6.0       # Persistent Caching
typer>=0.9.0           # CLI Tools
watchdog>=3.0.0        # File Watching für Development